#include <iostream>
#include <string>
// Tell if g1 is stored in more bytes than g2

using namespace std;

bool nagyobb;


int main() {
  char g1 = 'a';
  double g2 = 12.345;
  bool (nagyobb) = sizeof(g1) > sizeof(g2);
  	  if nagyobb == true {
  		  cout << igaz;
  	  }
  	  else {
  		  	 cout << hamis;
  return 0;
}
